[build]
  # Si tu package.json principal está en la raíz, usa solo npm install
  command = "npm install" 
  publish = "public"

[functions]
  directory = "public/server" # Donde residen tus archivos .js de Netlify Functions
  node_bundle = true