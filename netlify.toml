[build]
  command = "npm install && cd server && npm install"
  publish = "public"

[functions]
  directory = "server"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200